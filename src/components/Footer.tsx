export const Footer = () => {
  return (
    <div
      className="bg-dark"
      style={{
        display: "flex",
        flexDirection: "row",
        justifyContent: "center",
        color: "white",
      }}
    >
      Recording the collective memory
    </div>
  );
};

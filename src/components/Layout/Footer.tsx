export const Footer = () => {
  return (
    <div
      className="bg-dark"
      style={{
        display: "flex",
        flexDirection: "row",
        justifyContent: "center",
        color: "#222222",
        height: "50px",
      }}
    >
      Recording the collective memory
    </div>
  );
};
